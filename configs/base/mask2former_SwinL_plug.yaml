_BASE_: "${MASK2FORMER_ROOT}/configs/coco/instance-segmentation/swin/maskformer2_swin_large_IN21k_384_bs16_100ep.yaml"

MODEL:
  SEM_SEG_HEAD:
    NUM_CLASSES: 1
  MASK_FORMER:
    NUM_OBJECT_QUERIES: 100

SOLVER:
  IMS_PER_BATCH: 2
  BASE_LR: 0.0001
  MAX_ITER: 6000
  STEPS: (3600, 5200)

INPUT:
  IMAGE_SIZE: 1024
  MIN_SCALE: 0.1
  MAX_SCALE: 2.0

DATASETS:
  TRAIN: ("plug_train_merged",)
  TEST: ("plug_train_merged",)
