_BASE_: "mask2former_SwinL_plug.yaml"

MODEL:
  MASK_FORMER:
    # Query-aligned shape prior fusion (M2F-QSP)
    PRIOR_ON: True
    PRIOR_PATH: "/home/user/sjw/Yolo_pointrend/detectron2/plug_canonical_prior.npy"
    PRIOR_ALPHA: 1.0
    PRIOR_LOSS_WEIGHT: 1.0
    PRIOR_ALIGN_TYPE: "similarity"
    PRIOR_USE_BANK: True
    PRIOR_BANK_SIZE: 4
    PRIOR_DEBUG: False

    # 与当前 R50-QSP 方案保持一致：优先做 QSP 消融
    SDF_WEIGHT: 0.0
    GEOLOSS_WEIGHT: 0.0
