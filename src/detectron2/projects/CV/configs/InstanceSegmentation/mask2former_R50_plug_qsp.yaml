_BASE_: "mask2former_R50_plug.yaml"

MODEL:
  MASK_FORMER:
    # Query-aligned shape prior fusion (M2F-QSP)
    PRIOR_ON: True
    PRIOR_PATH: "/home/users1/sjw/cursor/workspace/outputs/plug_prior/plug_canonical_prior.npy"
    PRIOR_ALPHA: 1.0
    PRIOR_LOSS_WEIGHT: 1.0
    PRIOR_ALIGN_TYPE: "similarity"
    PRIOR_USE_BANK: False
    PRIOR_BANK_SIZE: 1
    PRIOR_DEBUG: False

    # v1 建议先保留已有的几何/形状辅助监督接口，便于后续消融
    SDF_WEIGHT: 0.0
    GEOLOSS_WEIGHT: 0.0

